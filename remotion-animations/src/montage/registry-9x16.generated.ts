// GENERADO por `npm run montage -- build` desde
// remotion-animations/projects/montage-*/montage-build.9x16.json.
// No editar a mano: los cambios se pierden en la siguiente compilacion.
import type {MontageVideoProps} from "./schemas";

export type MontageBuildEntry = {
  id: string;
  slug: string;
  build: MontageVideoProps;
};

export const montageBuilds9x16: MontageBuildEntry[] = [
];
